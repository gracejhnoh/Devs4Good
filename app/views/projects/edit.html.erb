<div class='form'>
  <h2 class='page-title'>Edit Your Project Here</h2>

  <%= form_for @project, url: organization_project_path do |f| %>
    <%= f.label :title, 'Project Title' %>
    <%= f.text_field :title %>

    <%= f.label :summary, 'Project Summary (max 255 characters)' %>
    <%= f.text_area :summary %>

    <%= f.label :description, 'Project Specs' %>
    <%= f.cktext_area :description, ckeditor: {toolbar: 'mini'} %>

    <%= f.label :time_frame, 'Project Needed By' %>
    <%= f.date_field :time_frame %>

    <%= f.label :contact_email, 'Contact e-mail for Project ' %>
    <p>(will be shown to any logged in developer viewing the project)</p>
    <%= f.email_field :contact_email %>

    <%= f.submit 'Edit Project', class: 'post-routes-button' %>
  <% end %>
</div>
